/**
 * Curated re-export of the approved Lucide icon set for UI chrome.
 *
 * Import icons from here, not directly from "lucide-react", so the
 * approved icon set stays enforceable in one place rather than drifting
 * as new features get added.
 */
export {
  Menu as MenuIcon,
  X as CloseIcon,
  ArrowRight as ArrowIcon,
  Phone as PhoneIcon,
  MapPin as MapPinIcon,
  Package as PackageIcon,
  Clock as ClockIcon,
  Check as CheckIcon,
  Truck as TruckIcon,
  ShoppingBag as ShoppingBagIcon,
} from "lucide-react";
